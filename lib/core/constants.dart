// Common constants, keys, and config values for the app
import 'package:flutter/material.dart';

class AppConstants {
  static const supportedLocales = [
    Locale('en'),
    Locale('ar'),
  ];
  static const defaultLocale = Locale('en');
  static const appTitle = 'Life Makers Pirates';
}
